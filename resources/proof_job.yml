resources:
  jobs:
    proof_job:
      name: proof_free_automation
      max_concurrent_runs: 1
      tasks:
        - task_key: proof_notebook
          notebook_task:
            notebook_path: ../src/proof_notebook.py
